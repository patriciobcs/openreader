{
  "name": "openreader-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && infisical run -- bun run dev",
    "build:web": "cd apps/web && infisical run -- bun run build",
    "lint": "cd apps/web && bun run lint"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
